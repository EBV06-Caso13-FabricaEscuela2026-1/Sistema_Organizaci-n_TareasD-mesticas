# Perfil activado en Render con SPRING_PROFILES_ACTIVE=prod
spring.h2.console.enabled=false

# La API sigue usando adaptadores en memoria; los datos se pierden al reiniciar el servicio.
# Cuando migres a PostgreSQL, enlaza la base en Render y configura datasource aquí o por variables de entorno.
